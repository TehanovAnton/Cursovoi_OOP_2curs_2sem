<Window x:Class="KursovoiProectCSharp.View.NewDeck"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursovoiProectCSharp.View"
        mc:Ignorable="d"
        Title="NewDeck" Height="300" Width="500">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label Content="Title"/>
            <TextBox Text="{Binding _Title}" Margin="3 3 3 3"/>
            <Button Content="Add" Height="20" RenderTransformOrigin="0.5,0.5" 
                    Margin="3 3 3 3" Command="{Binding AddDeck}"/>
        </StackPanel>
    </Grid>
</Window>
