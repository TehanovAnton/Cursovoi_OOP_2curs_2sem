<Page x:Class="KursovoiProectCSharp.View.AddCardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KursovoiProectCSharp.View" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:local1="clr-namespace:KursovoiProectCSharp.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddCardPage" Background="White">

    <Page.Resources>
        <local1:AddCardViewModel x:Key="def" QuestionText="." />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" DataContext="{StaticResource def}">
            <Label Content="Question"/>
            <TextBox x:Name="questionText" Text="{Binding QuestionText}" Margin="5 5 5 5"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Label Content="Answer"/>
            <TextBox  Grid.Row="1" Margin="5 5 5 5">
            </TextBox>
        </StackPanel>
    </Grid>
</Page>
